int main()
{
    // std::tm fecha = {};
    // fecha.tm_year = 100;
    // fecha.tm_mon = 0;
    // fecha.tm_mday = 3;
    // std::time_t fechaT= std::mktime(&fecha);
    // std::string nombre = "nuevoEvento", descripcion = "Sin descripcion";
    // Evento::Tipo tipo = Evento::TALLER;
    // Evento evento(nombre, tipo, descripcion, fechaT, fechaT, 10, 10, 10);
    // std::cout << evento.toString();
    return 0;
}
